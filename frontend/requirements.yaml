packages:
  "recharts": "Dashboard analytics charts and data visualization"
  "framer-motion": "Page transitions and smooth UI animations"
  "date-fns": "Human-readable date formatting"
  "clsx": "Utility for constructing className strings conditionally"
  "tailwind-merge": "Utility function to efficiently merge Tailwind CSS classes"

notes:
  - "External API is at https://digital-cmo-api.onrender.com (proxied via /api)"
  - "Implementing aggressive 5s timeout demo-fallback pattern for all hooks due to free-tier cold starts"
  - "JWT auth stored in localStorage as dcmo_token"
